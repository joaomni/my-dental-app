<section class="services">
  <h1 class="title">NOSSOS SERVIÇOS</h1>
  <p class="subtitle">Nossa lista de tratamentos</p>
  <main>
    {% for service in services -%}
    <card-services 
      title="{{ service.title }}" 
      description="{{ service.description }}" 
      link="{{ service.image_url }}"
      style="display: flex; justify-content: center;"
    >
    </card-services>
    {%- endfor %}
  </main>
  <a href="#cel" rel="canonical" class="button">Descubra mais</a>
</section>

<style>
  .services {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 2em;
    text-align: center;
    margin-top: 8em;
  }
  .services main {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2em;
    width: 100%;
  }
</style>